import aiosql
import asyncpg


async def tg_auth(user_id):
    "Authorizes student when he comes in bot."
    # handle operation errors
    pass
    # query = aiosql.from_path("greetings.sql", )
    # async with asyncpg.connect("greetings.db") as conn:
    #     with await query.


async def info_init(conn, name, patronymic, lastname, email, class_number):
    "Initializes information about student"
    pass
